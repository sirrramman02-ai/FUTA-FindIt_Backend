import test from 'node:test'; import assert from 'node:assert/strict'; import { scoreMatch } from '../src/services/matchingService.js'
test('matching rewards shared category, brand, color and location', () => { const match=scoreMatch({category:'Laptop',brand:'HP',color:'Black',location:'Chemistry Laboratory',keywords:['hp','laptop','sticker'],description:'black hp laptop sticker'}, {category:'Laptop',brand:'HP',color:'Black',location:'Chemistry Laboratory',keywords:['hp','laptop','chemistry'],description:'found black hp laptop'}); assert.ok(match.score >= 75); assert.ok(match.reasons.includes('Same category')) })
